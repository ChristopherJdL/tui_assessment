using System.IO;

namespace Tui.Assessment.FileReaders
{
    public partial class FileReader
    {
        /// No modification is to be done to the library to read an XML since XML is also text, and can be read by
        /// a previous implementation of ReadFile
    }
}